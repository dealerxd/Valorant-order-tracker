# ---- Supabase ----------------------------------------------------------
# Same project as the old panel/ and the Python tracker bot.
NEXT_PUBLIC_SUPABASE_URL=https://yhrvpgkxywwgeelhjszb.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_zwHVMn87dm7xJ1aB-_p7Xg_iUypsY-L
# Server only — never expose. Used by the /api/fx cron.
SUPABASE_SERVICE_ROLE_KEY=

# ---- FX (USD/EUR -> TRY) ----------------------------------------------
# Optional. Left blank the /api/fx route falls back to open.er-api.com,
# which needs no key.
FX_API_KEY=

# ---- Guards ------------------------------------------------------------
# Vercel sends `Authorization: Bearer $CRON_SECRET` on scheduled invocations.
CRON_SECRET=
# HMAC key for the customer /track/[token] links (surface not built yet).
TRACK_LINK_SECRET=

# ---- Tracker -----------------------------------------------------------
# The Riot/HenrikDev polling stays in the Python bot (repo root, Railway).
# The panel only reads tracker_state / tracker_matches, so no key is needed
# here. Kept for reference if the tracker is ever ported into /api/tracker.
# RIOT_API_KEY=
